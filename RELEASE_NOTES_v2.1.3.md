# AION2 Dashboard v2.1.3

发布日期：2026-02-27

## 本次版本定位

- `v2.1.3` 为当前稳定可用版本。
- 在保留 `v2.1.2` OCR 修复成果的基础上，补齐工坊卡片排序热修。

## 功能与修复

- 工坊卡片顺序修复：
  - 修复“做装模拟器”虽已在渲染顺序靠前，但被 `order-*` 样式覆盖导致仍显示在下方的问题。
  - 现已固定为工坊主区最上方，OCR 与市场分析顺延。
- 稳定性延续：
  - 保持 `v2.1.2` 的 ONNX 模型路径修复（优先 `app.asar.unpacked`）。
  - 保持 `latest.yml` 一致性校验守卫，避免自动更新 checksum mismatch。

## 回归验证

- 已执行 `npm run test:smoke`
- 已执行 `npm run dist:win`

